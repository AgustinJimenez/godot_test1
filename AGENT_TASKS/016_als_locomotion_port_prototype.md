# 016: ALS Locomotion Port Prototype

## Overview
Explore porting ALSHost's (UE5/ALS-Community) third-person locomotion feel — grounded movement, blended directional animation, rotation modes, basic jump/fall — onto a from-scratch Godot character in a brand-new, isolated scene. Not a rewrite of the existing `actors/player/*` system: that system (player.gd/player_body.gd + the foot IK stack) has two open bugs (`013`, `014`) and its own architecture already; this prototype lives independently so it can't regress that work, and so we're free to structure movement/animation the ALS way without fighting the existing one.

## Why this is hard, and what's already solved
- **Not hard**: getting ALS-style animations into Godot. `tools/retarget_cli.gd` + `HumanoidRetargeter` already proved this (task done in this session — walk + 3 sword-combo clips retargeted onto MotusMan, see `assets/models/als_retarget_test/`).
- **Hard part**: ALS's real complexity is in `ALSCharacter.cpp`'s AnimGraph-feeding logic — blendspace selection from speed/direction, rotation mode (Velocity Direction vs Looking Direction vs Aiming), lean/orientation warping, turn-in-place, foot IK. None of that is copyable code; it has to be re-derived in GDScript against Godot's `AnimationTree`/`AnimationNodeBlendSpace2D`, `CharacterBody3D`.
- **Source material is no longer the bottleneck**: all 126 `AnimSequence` assets under ALSV4_CPP's mannequin skeleton (locomotion 8-way walk/run/crouch, transitions/stops, turn-in-place, jump/fall/land, mantle/land-roll/get-up, lean/base poses, plus weapon-overlay/aim-offset poses) have been exported via a batched `unreal.AssetExportTask` python script (looped over all 126 paths in one `py` console call — `run_python`'s one-call-per-asset `export_fbx` op would have meant 126 separate tool calls) and copied into `assets/models/als_mannequin_standalone/full_set/`. Verified: all 126 re-imported cleanly with real Skeleton3D/AnimationPlayer/bone data (no collapsed-to-root failures). There is no dedicated "Idle" clip — ALS's idle is just holding `ALS_N_Pose` (a static base pose) plus an additive sway (`ALS_N_SecondaryMotion`), not a baked cycle; the prototype will need to approximate idle the same way (hold a pose, optionally blend in the additive) rather than looking for an idle animation that doesn't exist.
- **Animation source rule**: every animation this prototype plays must be exported from ALSHost and retargeted via the existing pipeline — never reuse this Godot project's own pre-existing clips (the pistol_starter/Mixamo pack's `W1_*`/mocap animations, or anything already under `assets/models/pistol_starter/Animation/`), even to fill an obvious gap like idle or run. If a needed clip doesn't exist yet in `als_mannequin_standalone/`, it has to be exported from ALSHost first, not borrowed. The character *mesh/skeleton* (`W1_Stand_Aim_Idle_IPC.fbx`'s embedded rig, see Phase 1) is fine to reuse — it's only the animation data that must stay ALSHost-only.

## Planned Deliverables (phased — stop and reassess after each)

1. **Scaffold**: new scene + script under `tests/manual/als_locomotion_prototype/` (or `actors/als_prototype/` if it graduates past "manual test"), a `CharacterBody3D` with a capsule collider and the **same character `player_body.gd` actually uses by default** — `res://assets/models/pistol_starter/Animation/In-Place/W1_Stand_Aim_Idle_IPC.fbx` (its embedded mesh/skeleton is what's really on screen in `foot_ik_preview.tscn`, since no `characters/*.json` catalog entry currently overrides `player_body.gd`'s `@export var character_scene` default — `MotusMan_v55.fbx` is a separate standalone file from the same pack, not what's actually rendered there). No dependency on `actors/player/*` code itself.
   - Also build varied test terrain in this scene the same way the foot IK test suite does — not by hand, reuse the existing procedural builders: `foot_ik_ramp_matrix_check.gd`'s `_build_ramp()` (CSGBox3D ramps at 15/30/45°, already parameterized by angle) and `foot_ik_stair_surfaces.gd`'s stair builder (parameterized by `step_count`/`step_height`, so a few flights at different step heights are cheap to add). Note: `foot_ik_preview.tscn` itself is flat-floor only — the ramps/stairs live in these separate dedicated check scenes, not in the preview scene.
2. **Core movement state**: grounded/falling state, walk/run/sprint speed tiers, acceleration/deceleration curves, gravity — a minimal GDScript re-implementation of `ALSCharacterMovementComponent`'s essentials, not a line-for-line port.
3. **Rotation modes**: Velocity Direction (body faces move direction) and Looking Direction (body faces camera/input forward, independent of move direction) — ALS's two baseline modes; skip Aiming mode for this phase.
4. **Animation blending**: `AnimationTree` with a 2D blendspace (forward/strafe or speed/direction) driven by the character's local-space velocity, using retargeted ALS clips. Requires exporting additional source clips from ALSHost (idle, run cycles, directional strafes) via the existing retarget CLI pipeline before this step can start for real.
5. **Jump/fall**: minimal airborne state + landing blend.
6. **Stretch, only if 1-5 feel right**: turn-in-place, reusing `player_foot_ik_modifier.gd`'s foot IK approach (don't fork a second IK implementation from scratch), basic mantling akin to `AGENT_TASKS/001_ledge_mantling_and_vaulting.md`.

## Explicit non-goals for this task
- Not touching `actors/player/*` or its foot IK stack.
- Not porting combat/melee/weapon systems — locomotion only.
- Not chasing full ALS parity (aim offsets, distance matching, ragdoll-getup) — see the open-ended discussion this task followed from.

## Open questions to resolve before/while starting
- Whether the eventual home for this is a permanent new character type in `characters/` (via `CharacterCatalog`) or stays a standalone manual test scene.

## Source file conversion tracker (real ALS-Community-UE5 C++, `Plugins/ALS-Community-UE5/Source/ALSV4_CPP/`)
Goal: as close a port as actually possible - the table below is the ground truth for what's been read, what's been ported, and what's provably not portable (Blueprint-only or data-curve-asset-only, not present anywhere in the C++ source). Update this table whenever a file gets read or a new mechanism gets ported, rather than re-deriving "what's left" from memory.

| File | Read? | Portable logic ported? | Notes |
|---|---|---|---|
| `Private/Character/ALSBaseCharacter.cpp` (+ `.h`) | Yes | Yes - rotation modes (`SmoothCharacterRotation`, real 800/500 deg/s interp speeds, real 150cm/s moving-rotation gate, sprint-snaps-to-velocity in Looking Direction), gait speed-hysteresis concept, `EventOnLanded` fall-speed-threshold concept (exact ragdoll/breakfall thresholds not applicable - no ragdoll/breakfall mechanic exists here) | Most of ALS's real character logic lives here, not in `ALSCharacter.cpp` |
| `Private/Character/ALSCharacter.cpp` (+ `.h`) | Read (confirmed thin) | N/A | Per the research pass, this is a thin subclass; the real logic is in `ALSBaseCharacter.cpp` above |
| `Private/Character/ALSCharacterMovementComponent.cpp` (+ `.h`) | Yes | No - confirmed NOT portable | Accel/braking-deceleration/friction all read a per-character `UCurveVector` (`CurrentMovementSettings.MovementCurve`), no fixed numbers exist in C++ to port. `ACCELERATION`/`DECELERATION` remain this prototype's own flat constants |
| `Private/Components/ALSMantleComponent.cpp` (+ `.h`) | Yes | **Yes, including the real curve-driven execution now** - the 125cm Low/High height-split threshold AND `MantleUpdate`'s actual `PositionCorrectionCurve` math (see the status log below for how) |
| `Private/Character/Animation/ALSCharacterAnimInstance.cpp` (+ `.h`) | **Read - deep pass done** | **Yes, everything portable that has a live consumer here** | This is where far more real logic lived than the earlier "it's all Blueprint" assumption suggested. **Ported**: `TurnInPlaceCheck`/`TurnInPlace` (see the turn-in-place row below), `ShouldMoveCheck` (150cm/s), and the definition of `AimingValues.AimingAngle.X` as the **raw** (unsmoothed) aim-vs-actor yaw delta — checked deliberately, because the separate `SmoothedAimingAngle` exists right beside it and using the wrong one would have been an invisible fidelity bug; the smoothed one feeds only aim-offset blending, which this prototype has no equivalent of. **Not portable (curve assets)**: `UpdateLayerValues` is 100% curve reads (`Layering_*`/`Mask_*`/`BasePose_*` baked into ALS's own animations — our retargeted clips carry none of them), as are `CalculateStrideBlend`, `CalculateStandingPlayRate`, `CalculateCrouchingPlayRate`, `CalculateDiagonalScaleAmount`, and `CalculateAirLeanAmount`. **Portable but deliberately not ported, with reasons**: `CalculateVelocityBlend` is ALS's replacement *for* a blendspace (its own comment: "used in a BlendMulti node to produce better directional blending than a standard blendspace") — porting it means ripping out the working `BlendSpace2D` for a structural rewrite with no clear win; `UpdateFootIK`/`SetFootLocking`/`SetFootOffsets`/`SetPelvisIKOffset` are ALS's own foot IK, which this prototype deliberately replaces with this project's existing `PlayerFootIKModifier`; `DynamicTransitionCheck` needs additive transition clips and this project's additive retarget path is known-broken (see the status log); `CalculateLandPrediction` has real numbers (fall speeds -200/-4000 cm/s, trace length mapped `{0,-4000}→{50,2000}`) but its final weight comes from `LandPredictionCurve` and there's no land-prediction blend here to feed; `RotateInPlaceCheck` needs an Aiming rotation mode to exist first. **Also ported**: `UpdateAimingValues`' `SpineRotation.Yaw = AimingAngle.X / 4.0` — see the spine-twist status entry below. **Known-portable and still TODO**: `OnJumped`'s `JumpPlayRate = GetMappedRangeValueClamped({0,600},{1.2,1.5}, Speed)` (needs an `AnimationNodeTimeScale` inserted into the Airborne branch to have anything to drive). |
| `Private/Library/ALSMathLibrary.cpp` (+ `.h`) | **Read - all 6 functions** | **Partly - the 2 that had a real job here** | Small file, no curve dependencies. **Ported**: `CapsuleHasRoomCheck` (real ALS's actual "can the character stand here" test - sweeps a sphere of the capsule's own radius along the capsule's cylindrical segment, `half_height - radius` above and below centre, which traces the capsule's exact volume; replaces an invented zero-width downward raycast in the mantle path that could thread through a gap the 0.7m body can't fit) and `GetCapsuleLocationFromBase` (base/feet → capsule centre, with the real **2cm ZOffset** lift `ALSMantleComponent.cpp` step 3 passes at its own call site, which also passes `HeightOffset=0`/`RadiusOffset=0`). **Deliberately not ported, with reasons**: `GetCapsuleBaseLocation` has no work to do - this prototype's `_body` origin already sits at the capsule base (the `CollisionShape3D` carries the +half-height offset), which is exactly what ALS calls the capsule base location; `MantleComponentLocalToWorld` exists to keep a mantle target relative to a *moving* component/platform, and this prototype's ledges are static geometry; `AngleInRange`+`CalculateQuadrant` have exactly one consumer, `CalculateMovementDirection()`, which picks one of 4 *discrete* directional cycle states in the AnimBP's `(N) Directional States` - this prototype uses a continuous `BlendSpace2D` keyed by local velocity that already covers that role, so porting the enum would be dead code nothing reads (real thresholds recorded here in case that changes: FR/FL/BR/BL = `70`/`-70`/`110`/`-110` with a `5°` buffer; note also that `CalculateMovementDirection` returns `Forward` unconditionally while sprinting or in Velocity Direction mode, both of which are already emergent here since the body faces its velocity in both cases). |
| `Private/Character/ALSPlayerCameraManager.cpp` / `ALSPlayerController.cpp` / `Animation/ALSPlayerCameraBehavior.cpp` | **Not read** | Not ported | Real ALS's actual camera-follow/collision/lag system - this prototype's camera is a from-scratch simple orbit cam, not derived from these at all |
| `Private/Components/ALSDebugComponent.cpp` | **Not read** | N/A | Debug-only tooling, not gameplay logic - low priority to ever read |
| `Private/AI/ALSAIController.cpp`, `ALS_BTTask_GetRandomLocation.cpp`, `ALS_BTTask_SetFocusToPlayer.cpp` | **Not read** | N/A | AI - out of scope, this prototype is player-only |
| `Private/Character/Animation/Notify/*.cpp` (5 files: footstep, camera shake, grounded-entry-state, early-blend-out, movement-action, overlay-override) | **Not read** | N/A | Animation-notify plumbing, not core locomotion decision logic - low priority |
| Turn-in-place's actual trigger logic | **Read live, and re-read in C++** | **Yes, now ported for real** | Earlier claim in this doc ("Blueprint-only, unreadable") was wrong on two counts. First, corrected: the ALSHost UE5 editor is a live running process with ClaudeUnrealMCP attached, so `read_state_machine` (with the real bound-graph names, not the cosmetic node titles - see below) can inspect `ALS_AnimBP` live. Second, once actually read: the AnimGraph's `(N) Locomotion States` state machine (inside the `BaseLayer` Anim Layer function) just gates its `(N) Not Moving` → `(N) Rotate Left/Right 90` transitions on two plain booleans, `Grounded.bRotateL`/`bRotateR` - and those booleans, plus the real turn/delay/clip-selection logic, are set in genuine, fully portable **C++** in `ALSCharacterAnimInstance.cpp` (`TurnInPlaceCheck`/`TurnInPlace`/`RotateInPlaceCheck`), not Blueprint at all. The AnimGraph was never hiding the real decision logic - it only reads the C++-computed result. Ported into `_update_turn_in_place_check()`/`_maybe_start_turn_in_place()` with the real default thresholds from `ALSAnimationStructLibrary.h`'s `FALSTurnInPlaceValues`: `TurnCheckMinAngle=45°`, `Turn180Threshold=130°` (this prototype already had 130 right by luck), `AimYawRateLimit=50°/s`, `MinAngleDelay=0s`/`MaxAngleDelay=0.75s` mapped over the 45°-180° angle range (a bare-threshold angle turns almost instantly, a fully-behind angle waits up to 0.75s - guards against a quick camera flick committing to a turn). Real ALS also only runs this delay-gated check for **Looking Direction** mode while idle (in Velocity Direction mode idle, the body doesn't auto-face the camera at all) - this prototype previously ran the old instant-threshold version for Velocity-Direction-idle only and never used a delay for Looking-Direction-idle at all (it just span continuously toward the camera every frame, even standing still); both are now fixed to match real ALS's structure, with the Velocity-Direction-idle usage kept as an explicitly-labeled non-ported pragmatic UX addition (real ALS has no idle facing-catch-up in that mode). `RotateInPlaceCheck` (the separate always-rotating-while-aiming variant, `RotateMinThreshold=-50°`/`RotateMaxThreshold=50°`/aim-yaw-rate-scaled play rate) was also found but not ported - this prototype has no Aiming rotation mode/first-person view yet for it to apply to. |

## Blueprint assets involved (also real ALS logic, not covered by the C++-only table above)
Plugin content under `Content/AdvancedLocomotionV4/Blueprints/` - readable live via ClaudeUnrealMCP against the running ALSHost editor (see correction above), not from disk.

| Blueprint | Read? | Relevance |
|---|---|---|
| `CharacterAssets/MannequinSkeleton/ALS_AnimBP` | **Read** (`AnimGraph` root + `BaseLayer`/`OverlayLayer`/`(N) CycleBlending`/`(CLF) CycleBlending`/`AimOffsetBehaviors`/`FootIK`/`HandIK`/`LayerBleding` Anim Layer functions) | Confirmed structure: `AnimGraph` holds ~12 sibling top-level `AnimGraphNode_StateMachine`s (Ragdoll State, Aim Offset Behavior States, Main Movement States, Main Grounded States, Overlay States, plus per-layer Locomotion Cycles/States/Directional States in both `(N)` normal and `(CLF)` crouch/prone-ish variants) - NOT one monolithic graph. `Main Grounded States` is Normal↔Crouch(CLF)/Roll only; the actual Idle/Move/**Turn-in-place** states live one level deeper, inside `(N) Locomotion States` (reached via a cached-pose node off `(N) Standing`). Its `(N) Not Moving`→`(N) Rotate Left/Right 90` transitions gate on plain `Grounded.bRotateL`/`bRotateR` booleans computed in C++ (see the source-file table above) - the AnimGraph itself carries no turn-angle math, just the resulting bool. **Tool gotcha**: `read_state_machine`'s `state_machine_node_name` filter matches the state machine node's *bound graph* name/title exactly as it substring-matches against `GetNodeTitle()`/`EditorStateMachineGraph->GetName()` - but for most of these nodes that lookup silently fails to find matches even for their own exact displayed title (only "Ragdoll State" and the generic "State Machine" substring worked directly); the reliable path was locating each node's real containing Anim Layer function first: calling `read_function_graphs` with no `name` filter dumps ALL of the Blueprint's top-level function graphs in one call (`AnimGraph`, plus every Anim Layer function - `BaseLayer`, `OverlayLayer`, `(N)`/`(CLF) CycleBlending`, `AimOffsetBehaviors`, `FootIK`, `HandIK`, `LayerBleding` - each correctly labeled with its own `graph_name`), which is how e.g. `(N) Locomotion States` was found to actually live in `BaseLayer`, not `AnimGraph`. This doesn't contradict the `AGENTS.md` recursive-`GetSubGraphs()` gotcha - that's about a state machine's own internal states/transitions (still only reachable via `read_state_machine`), a level deeper than the sibling top-level function graphs this was actually about. Passing the correct real `graph_name` (not assuming `"AnimGraph"`) is what made the `state_machine_node_name` filter start working. |
| `Data/DataTables/MovementModelTable` | **Read, and ported** | Not a Blueprint but belongs with them: this is the asset side the C++ defers to. **The real gait speeds live here and nowhere else** - `WalkSpeed`/`RunSpeed`/`SprintSpeed` are all `0.0f` in C++, filled in from this table at runtime, which is exactly why the earlier "no universal numbers exist in source" finding was true but incomplete. "Normal" row, Standing: **175 / 375 / 650 cm/s**. (Crouching 150/200/300; the Responsive and Sluggish rows are alternate movement models, and Aiming-Standing differs only in walk speed, 165.) Ported as 1.75/3.75/6.5 m/s, replacing invented 3.0/6.0 - and this also surfaced that the prototype had **only two gaits where ALS has three**, with ALS's default gait being *Running*, not walking. The `Run_*` blendspace ring also moved from SPRINT_SPEED to RUN_SPEED, since those are `ALS_N_Run_*` clips authored against RunSpeed; ALS has no separate sprint clip set at all (it reuses the run cycle at a higher play rate), so sprint simply saturates the outer ring. |
| `Blueprints/CharacterLogic/ALS_CharacterBP` | **Read (class defaults), partly ported** | Thin on *logic*, but carries real configuration - same lesson as `MantleComponent`. Ported: **`DesiredRotationMode = LookingDirection`** (this prototype defaulted to Velocity Direction, which was simply wrong) and **`ThirdPersonFOV = 90`** (Godot's `Camera3D` default is 75, so framing was visibly tighter than ALS's; the rest of the camera is still a from-scratch orbit cam). Confirmed rather than newly learned: **`DesiredGait = Running`** (independently validates making RUN_SPEED the no-modifier gait), `MovementModel` points at the `MovementModelTable` "Normal" row (validates the row used for the speeds), and `BreakfallOnLandVelocity=700`/`RagdollOnLandVelocity=1000` match the earlier C++ read exactly. Noted, not applicable here: `bRightShoulder=True`, `BaseEyeHeight=64`/`CrouchedEyeHeight=32`, `RollDoubleTapTimeout=0.3`, `JumpMaxCount=1`, `LookUpDownRate`/`LookLeftRightRate=1.25`, `bUseControllerRotationYaw=False`. |
| `Blueprints/CameraSystem/ALS_PlayerCameraBehavior` | **Read, and ported** | The camera's entire tuning lives here, not in the C++: `ALSPlayerCameraManager.cpp` reads every value through `GetCameraBehaviorParam(name)`, i.e. curves this AnimBP sets. Its `Main Camera States` machine has exactly three states - Velocity Direction, Looking Direction, Aiming - each a stack of `ModifyCurve` nodes. **Key tool finding**: the values are pin defaults (visible to `read_function_graphs`) but the `CurveNames` array saying which curve each slot is, is NOT - it needs `run_python`'s `inspect_graph_node`. Without that the numbers are an unlabelled list. All values and the port are in the status log below. |
| `Blueprints/CameraSystem/ALS_PlayerCameraManager`, `Camera`, `Camera_Skeleton` | **Not read** | Real ALS's actual camera rig. `Camera_Skeleton` in particular is ALS's known technique of driving camera lag/offset/collision through an animated virtual skeleton (a "camera AnimBP"), not pure C++ - the camera system is likely MORE Blueprint-driven than C++-driven, so reading only `ALSPlayerCameraManager.cpp` (as the earlier "next reads" list suggested) would still miss most of the real behavior |
| `Blueprints/Components/MantleComponent` | **Read, and ported** | **Not thin at all** - the earlier guess that the plugin's non-AnimBP Blueprints are "thin config subclasses" is wrong here. The C++ only declares `FALSMantleTraceSettings`; every actual number lives on this Blueprint's class defaults, so the asset side was again the only possible source. Real values, now ported: **Grounded** `MaxLedgeHeight=250 / MinLedgeHeight=50 / ReachDistance=75`, **Falling** `150 / 50 / 70`, both with `ForwardTraceRadius=DownwardTraceRadius=30`. This prototype had collapsed all of that into one invented profile (40/200/60), so it lacked ALS's real distinction that **a standing mantle reaches a 2.5m ledge but one caught mid-jump only reaches 1.5m**. Also confirms `ALS_MANTLE_HIGH_THRESHOLD=1.25` was already correct - it is `Mantle_2m_Default.LowHeight`. (`AutomaticTraceSettings` 80/40/50 drives ALS's continuous auto-detect mantle, which this prototype doesn't have; `AcceptableVelocityWhileMantling=10` rejects mantling onto a fast-moving platform, not applicable to static test ledges.) |
| `Blueprints/AnimModifiers/Calculate_RotationAmount` (+ `Create_Curves`, `Copy_Curves`, `Create_LayeringCurves`) | **Not read** | Editor-time-only (bakes the `RotationAmount`/layering curves into animations at import time, not runtime logic) - confirms the turn-in-place curve is procedurally generated, but not itself a runtime decision to port |
| `Blueprints/Misc/Sprint_CameraShake`, `Blueprints/GameModes/ALS_GameMode_SP` | **Not read** | Low priority - camera-shake juice and game-mode setup, not core locomotion |
| `Blueprints/Input/*`, `Blueprints/UI/*`, `Blueprints/CharacterLogic/AI/*`, `Blueprints/Components/DebugComponent` | Not read, not planned | Input mapping contexts/actions, HUD, AI, debug tooling - out of scope for a locomotion port |

**Next highest-value work, in priority order.** Every C++ file that carries portable locomotion logic has now been read - turn-in-place, `ALSMathLibrary.cpp` and the `ALSCharacterAnimInstance.cpp` deep pass are all done - and so has every asset-side source that carried real numbers (`MovementModelTable`, `MantleComponent`, `ALS_CharacterBP`), so what's left is implementation, not reading (the jump play rate, formerly #1, and crouch are both now done): (1) the camera system - now understood to mean the Blueprint camera rig (`Camera_Skeleton` etc.) at least as much as `ALSPlayerCameraManager.cpp`, a larger effort that would mean rebuilding the camera to match ALS's real follow/lag/collision behavior instead of the current simple orbit cam.

## Status
- Phase 1 scaffold built: `tests/manual/als_locomotion_prototype/` — `CharacterBody3D`, WASD movement, Velocity Direction rotation, gravity/jump, trailing camera, 3 ramps (15/30/45°) + 2 stair flights (0.15m/0.3m steps), all independent of `actors/player/*`.
- `tools/retarget_cli.gd` gained a `target_model=` mode (auto-detects the bone map via the same fallback `PlayerBody._detect_target_humanoid_map()` uses, no `characters/*.json` manifest needed).
- All 126 raw ALS clips sit in `assets/models/als_mannequin_standalone/full_set/`, ready to retarget onto whatever rig is current as needed for Phase 2-6.
- **Character switched from the default player rig to Y Bot** (`assets/models/mixamo_characters/Y Bot.fbx`). Evaluated via `tests/manual/animation/als_retarget_preview.tscn` (repurposed for spot-checking candidate retarget targets, not a permanent MotusMan-only gallery — see that script's own header comment): X Bot was tried first but looks visibly feminine/androgynous next to the muscular ALS reference mesh, so Y Bot was picked instead. User confirmed walk/sprint look good in the prototype scene.
- **Phase 5 (jump/fall) done, basic version**: `ALS_N_JumpLoop`/`ALS_N_FallLoop` baked onto Y Bot (`*_on_ybot.res`). `AnimationTree` root is now a 2-state `AnimationNodeStateMachine` (`Grounded` holding the idle/walk/run 1D blendspace, `Airborne` holding a fall/jump 1D blendspace keyed by vertical-velocity sign), switched via `playback.travel()` on `is_on_floor()` edges — not an `advance_condition`, since the trigger is a one-frame edge. Verified via headless smoke tests (settle → jump → Airborne → land → Grounded transitions all confirmed).
  - **Gotcha worth remembering**: `AnimationNodeStateMachine` has no working `start_node` property in Godot 4.6 (the property doesn't exist; only `states/Start/...` sub-properties do, and an explicit `Start`→state transition with `ADVANCE_MODE_AUTO` did not change the observed initial state either). The state machine appeared to always start on `Airborne` regardless of node-add order. Fix: force it explicitly via `playback.start(...)` — but only once `AnimationTree` has processed at least one real (physics) frame; calling `.start()` synchronously in the same call that builds the tree has no effect (`parameters/playback` isn't live yet). Deferred the forced-start call to the first `_physics_process` tick instead.
- **Phase 3 (rotation modes) done**: added mouse-look (`_camera_pivot` yaw, previously fixed) and camera-relative movement input (both modes need it, ALS-standard third-person control). Looking Direction lerps the body to camera yaw every frame; Velocity Direction faces move direction, holds last facing when idle. `G` toggles mode, `Esc` releases the mouse, on-screen label shows current mode.
  - **Real bug found and fixed via user report + headless verification, not the first thing suspected**: reported as "character faces the camera" — first hypothesis (an FBX mesh-forward-axis quirk) was wrong and disproven by two independent probes (`Node3D` rotation→forward on the isolated model, and `HumanoidRetargeter._skeleton_rest_facing()` on Y Bot's rest pose — both showed a clean, unoffset -Z forward). The actual bug: Velocity Direction's yaw formula was `atan2(horizontal.x, horizontal.z)` — missing the negation `atan2(-x, -z)` that Godot's -Z-forward convention requires (already documented in this project's own `AGENTS.md`). Looking Direction's `camera_yaw` usage needed no correction at all. Confirmed via a headless test comparing actual velocity direction against the character's resulting forward vector (dot product -1.0 before the fix, +1.0 after) - don't trust a bone/rest-pose "facing" probe as proof of correctness for a *different* code path (movement-direction math) than the one it actually measured.
  - **A second, real 180° offset also existed** (confirmed after the above fix by live user re-test: "still inverse... must be turned 180") - the character mesh's true visual front did NOT match what either probe suggested at the abstract-transform level. Fixed on the *visual* layer per `AGENTS.md`'s own guidance ("configure asset-facing offsets on visual children, not on AI/collision/navigation parents"): `_character` is now a bare logical-facing wrapper `Node3D`, holding a separate `visual` child (the actual Y Bot instance) with a fixed `visual.rotation.y = PI`. Every rotation-mode/turn-in-place formula stays the textbook-correct Godot convention; only the rendered mesh gets the extra 180°.
- **Turn-in-place done** (stretch item, Phase 6): 4 one-shot clips (`ALS_N_TurnIP_{L,R}{90,180}`) baked onto Y Bot, added as `AnimationNodeStateMachine` states (`Grounded`↔`TurnL90`/`TurnR90`/`TurnL180`/`TurnR180`). Only triggers in Velocity Direction mode while idle and grounded (Looking Direction needs no turn-in-place — it already tracks the camera continuously every frame). The actual yaw reorientation is driven procedurally (linear interpolation timed to the clip's real length, same approach as jump/fall not needing root-motion decoding) rather than reading the clip's own hip rotation back out - picks the nearest-magnitude clip (90 for real angles up to 130°, else 180) rather than requiring an exact match, same tradeoff real ALS makes.
- **Basic mantling done** (stretch item, Phase 6; foot IK reuse was explicitly deferred — see below): pressing jump while grounded and facing a wall within `MANTLE_WALL_CHECK_DISTANCE` with a walkable surface on top in `[MANTLE_MIN_HEIGHT, MANTLE_MAX_HEIGHT]` triggers a mantle instead of a normal jump — two raycasts (a low wall-probe, then a downward probe past it to find the ledge top), then a procedural position lerp (same pattern as turn-in-place) from the current spot to the ledge-top point, timed to `ALS_N_Mantle_1m_RH`'s clip length. A dedicated 1m test ledge was added to the terrain (`_build_mantle_ledge`). **Found and fixed one real bug**: the first wall-probe used a fixed chest-height (1.2m) origin, which sailed clean over the 1.0m test ledge and never detected it — fixed by probing at `MANTLE_MIN_HEIGHT * 0.5`, low enough to hit the wall face of any ledge in the mantleable range regardless of its exact height. User confirmed it "kinda works" live.
- **Directional locomotion done** (closes a real Phase 4 gap flagged after initial completion — the plan called for a 2D blendspace, but only a 1D speed blendspace had actually been built, so strafing/backpedaling in Looking Direction mode still played the forward-walk clip). 10 more clips baked (`Walk_B/LF/RF/LB/RB`, `Run_B/LF/RF/LB/RB`), `Grounded`'s `AnimationNodeBlendSpace1D` replaced with an `AnimationNodeBlendSpace2D` — idle at the origin, a walk-speed ring and a run-speed ring of the same 6 directions, keyed each frame by the character-LOCAL (right, forward) velocity (`_character.transform.basis.inverse() * horizontal`), not world-space, so the blend is genuinely relative to the body's own facing. Godot 4.6's `AnimationNodeBlendSpace2D` has no separate "Directional" blend mode like some other engines (confirmed via property-list introspection - only Interpolated/Discrete/Carry) but the default Interpolated mode's triangulation already does the right thing. Verified via headless tests: Velocity Direction mode always blends to pure-forward regardless of world movement direction (correct - body chases its own movement, per that mode's own definition), Looking Direction mode correctly blends toward the strafe-right point when strafing.
- **Foot IK reuse done** (the previously-deferred Phase 6 stretch item — user asked to just implement everything remaining). `PlayerFootIKModifier` (`actors/player/player_foot_ik_modifier.gd`) turned out to need only 6 members off its `player_body` (`character`, `anim_player`, `locomotion_playback_scale`, `resolve_bone_name()`, plus inherited `get_parent()`/`get_world_3d()` — confirmed by grepping the whole ~5000-line system, not assumed), not the full `PlayerBody` class's cosmetics/inventory/retarget-caching surface. New file `tests/manual/als_locomotion_prototype/prototype_body_facade.gd` (`PrototypeBodyFacade extends PlayerBody`, `_ready()` overridden to a no-op) satisfies the modifier's static `player_body: PlayerBody` type requirement while staying otherwise inert — reuses `PlayerBody`'s own real `resolve_bone_name()` for free rather than reimplementing it. This same node doubles as the prototype's existing facing wrapper (`_character`).
  - **Real bugs found and fixed, both general (not prototype-specific) and low-risk to fix upstream**:
    1. A plain duck-typed `Node3D` facade compiled fine but left `player_body` silently `null` at the modifier's own `_ready()` (`Object.set()` silently no-ops rather than erroring when the runtime type doesn't match a script-declared static property type) — this is why the facade extends `PlayerBody` instead.
    2. `foot_ik_gait_tracker.gd`'s `update_animation_discontinuity()` called `AnimationPlayer.get_current_animation_position()` unconditionally, which errors (an engine-level `ERR_FAIL_COND`, not a script exception) whenever `current_animation` is unset — true for any `player_body` whose locomotion is driven by an `AnimationTree` instead of direct `anim_player.play()` calls, as this prototype's Grounded/Airborne blendspaces are. Added an `current_animation.is_empty()` guard before the read (`actors/player/foot_ik/foot_ik_gait_tracker.gd`) — real `PlayerBody` always has a `current_animation` set, so this is a no-op there; verified no regression by running `tests/manual/foot_ik/foot_ik_preview.tscn` headlessly for 90 frames after the change.
    3. Attaching the modifier to the skeleton before the character's `CharacterBody3D` root was itself added to the live scene tree hit Godot's "Parent node is busy setting up children" error (the modifier's own `_ready()` needs to `add_child()` its native-backend node) — real `PlayerBody` never hits this since its whole hierarchy already exists via its own `.tscn` before anything's `_ready()` runs; this prototype builds everything procedurally in one `_ready()`, so call order actually matters here. Fixed by moving `add_child(_body)` before the Foot IK attachment block in `_build_player()`.
  - Verified end-to-end via a headless run: no errors, character walks correctly with the modifier attached and actively solving (`[FOOT_IK_PERF]` diagnostic confirms real solve calls happening). Not yet visually confirmed live by the user - they asked for everything to be implemented for later review rather than checking each step this time.
- **Landing blend done** (closes the other Phase 5 gap - the plan asked for "airborne state + landing blend", only the airborne state actually existed). `ALS_N_Land_Light` baked onto Y Bot, added as a one-shot `Landing` state (`Airborne`→`Landing`→`Grounded`, same procedural-duration pattern as turn-in-place/mantle), triggered on a genuine on-floor landing edge instead of the bare `Airborne`→`Grounded` crossfade (kept as a fallback for other paths, e.g. the forced first-frame `start()`). Cancels early back to `Grounded` if the player starts moving mid-landing, same idea as turn-in-place's own move-cancel. Not gated by fall speed/height (always plays the light-landing clip - a heavy-vs-light split by fall distance would be the natural next step, not done here). Verified via a headless test: Airborne → Landing → Grounded transitions all confirmed.
- **Mantling polish done**: (1) character now faces the wall for the mantle's duration (captured at trigger time, lerped over the first third of the clip) instead of keeping whatever yaw it approached with; (2) a headroom raycast (standing-height clearance above the landing spot) rejects the mantle if something's in the way, so a low overhang can't pull the character up into it; (3) a running/jumping mantle now works too - pressing jump while already airborne (not just the standing jump-press) also checks for a ledge, matching real ALS letting a running jump catch a ledge. Re-verified end-to-end after these changes: mantle still triggers, completes, and lands correctly (headroom check didn't false-reject the test ledge).
- **Stair climbing fixed, after three real attempts, each one caught by a progressively stricter automated test.** `CharacterBody3D` has no built-in stair-stepping - a riser just reads as a wall to `move_and_slide()`. User report: "cannot walk on stairs" → "still happening" after attempt 1 → "make the test to walk over the 2 stairs" after attempt 2's own automated check turned out to be checking the wrong thing entirely.
  - **Attempt 1** (lift by `STAIR_STEP_HEIGHT` + widen `floor_snap_length`, trusting `move_and_slide()`'s own snap timing) looked fine on a *sparse*-sampled test (printing every 20 frames), which hid an obvious per-step glitch. A rewritten test logging every single `is_on_floor()` transition (not periodic snapshots) proved the real behavior: the 0.3m-step flight flickered `Grounded`↔`Airborne`↔`Landing` on literally every step (8 false→true edges, one per step) - `floor_snap_length` alone wasn't enough margin for a lift matching the full step height.
  - **Attempt 2** (a precise downward raycast to place the body exactly on the tread, replacing the snap-timing trust) reported "zero `on_floor` flickers" on the same edge-logging test - true, but for the wrong reason: the raycast's probe point used only that single frame's tiny velocity×delta displacement as its forward offset, nowhere near far enough to actually reach the step ahead, so it kept re-hitting the *flat floor short of the stairs* and resetting the body back to y=0 every frame - a stable, flicker-free, **completely stuck** loop. The "zero flicker" check only verified `on_floor` never blipped, not that the character was actually making progress; a character frozen at the base of the stairs trivially passes that same check. Caught only once the test was rewritten again to log actual per-frame position, at the user's explicit request for a real "walk over both stairs" test - it showed the exact stuck coordinates and zero velocity.
  - Also found and fixed a second real bug the position-logging made obvious: the capsule collider is 0.7m wide (radius 0.35) but the original stair tread depth was only 0.3m, so the body was already touching the *next* riser while standing on any single step, independent of height - `STEP_DEPTH` in `_build_stairs()` widened to 0.9m to give the capsule room.
  - **Real fix**: back to the simple attempt-1 lift (the raycast idea was the wrong tool - decoupled entirely from actually reaching the step), but instead of trying to make `is_on_floor()` never blip (attempts 1 and 2 both chased this and both failed for different reasons), the *animation* transition to `Airborne` is now debounced separately (`AIRBORNE_DEBOUNCE_FRAMES`, 12 consecutive off-floor physics frames before the state machine actually switches) - gravity and `is_on_floor()` itself stay fully reactive for real physics, only the visual state-machine transition waits. Verified via a combined test tracking both max height reached (1.351/2.400, matching the two flights' real heights) AND animation-state-change count (3-4 total, all at the genuine walk-off-the-top fall, zero per-step flicker) in the same run.
- **"True conversion" pass: read the real ALS-Community-UE5 C++ source (`ALSBaseCharacter.cpp`, `ALSCharacterMovementComponent.cpp`, `ALSMantleComponent.cpp`) for the first time in this task, rather than continuing to approximate from general ALS-genre knowledge.** Everything up to this point (including this doc's own earlier phase descriptions) was written from general knowledge, not the actual source - a fair question the user asked directly ("are you actually reading the ue5 files?"), answered honestly (no, not until this pass) before starting it. Findings and what was actually portable:
  - **Rotation modes now match the real formula.** `ALSBaseCharacter.cpp::SmoothCharacterRotation` is two-stage, not a single lerp: an internal `TargetRotation` chases the desired yaw at a **constant** angular speed (`RInterpConstantTo`, real value **800 deg/s** for Velocity Direction, **500 deg/s** for Looking Direction - exact numbers from source), then the actual character rotation **exponentially** chases `TargetRotation` (`RInterpTo`) at a per-character speed/aim-rate curve we don't have an asset for (approximated with the existing `ROTATION_SPEED` constant). Ported as `_smooth_character_rotation()` with a new `_rotation_target_yaw` state var mirroring ALS's own `TargetRotation` member. Also fixed two real behavioral gaps: Velocity Direction only updates rotation while `(moving && has input) || speed > 1.5 m/s` (`ALS_MOVING_ROT_SPEED_THRESHOLD`, exact 150cm/s value from `bCanUpdateMovingRot`) rather than on any nonzero speed as before; Looking Direction **snaps to face velocity while sprinting** (same target as Velocity Direction), only using camera yaw as the target when walking/running - confirmed by reading the source, not assumed, and this project had it wrong (always camera yaw) before. Re-verified both modes still pass the same alignment tests as before the rewrite.
  - **Mantle now has real ALS's Low/High TYPE split** at the actual **125cm threshold** from `ALSMantleComponent.cpp` (`MantleHeight > 125.0f`), baked `ALS_N_Mantle_2m` for the High case, added a second test ledge (1.8m) and a `MantleHigh` state alongside the existing `MantleLow`. Real ALS's actual mantle execution is structurally different and NOT ported - it's a genuinely curve-driven Timeline sampling an authored `PositionCorrectionCurve` per mantle asset (even per-overlay-state, i.e. a different mantle animation while holding different weapons), not this prototype's `smoothstep()` position lerp - documented as a known, deliberate simplification rather than silently claimed as equivalent. Verified: Low ledge (1.0m) correctly triggers `MantleLow` and settles at y=1.0; High ledge (1.8m) triggers `MantleHigh` and settles at y=1.8.
  - **Landing now picks Light vs Heavy by fall speed**, conceptually matching real ALS's `EventOnLanded` (which uses fall-speed thresholds for ragdoll/breakfall/normal-landing decisions, real values 1000cm/s and 700cm/s) - but the actual **Light vs. Heavy landing POSE choice is itself an AnimBP decision in real ALS too**, not a C++ number, so `LAND_HEAVY_SPEED_THRESHOLD := 5.0` is this prototype's own chosen value (documented as such, not claimed as ported), picked comfortably above the 6.0 `JUMP_VELOCITY` so an ordinary jump always lands light. `_last_fall_velocity_y` is captured every airborne frame (not read at the landing edge itself, since `move_and_slide()` zeroes `velocity.y` the instant it resolves floor contact) and the `_land_node`'s `.animation` is swapped at trigger time rather than needing two separate state-machine states. Verified via a headless test with a proper wait-for-Grounded between the two cases (an earlier version of the same test raced ahead before the first landing had actually finished, silently re-reading stale state): small hop (-3.3 m/s) → `land_light`, 6m fall (-14.7 m/s) → `land_heavy`.
  - **Turn-in-place's real trigger turned out to BE portable after all - this doc's own earlier claim above was wrong.** The `RotationAmount` AnimCurve is real (applied via `ALSBaseCharacter.cpp` to drive root rotation smoothly during the clip), but the *decision* of when to start a turn and which clip is genuine C++ in `ALSCharacterAnimInstance.cpp` (`TurnInPlaceCheck`/`TurnInPlace`), not Blueprint - confirmed by actually reading `ALS_AnimBP` live via ClaudeUnrealMCP (see the Blueprint-assets table above) and finding its turn states gated on plain C++-computed booleans. Ported for real: `ALS_TURN_CHECK_MIN_ANGLE_DEG=45°`, `TURN_180_THRESHOLD_DEG=130°` (unchanged, was already correct), `ALS_AIM_YAW_RATE_LIMIT_DEG=50°/s`, and a genuine `MinAngleDelay=0s`/`MaxAngleDelay=0.75s`-mapped delay accumulator (`_update_turn_in_place_check()`) replacing the old instant-threshold `TURN_90_THRESHOLD_DEG=50°` guess. Also fixed a real structural gap found in the same pass: real ALS only runs this idle turn-in-place check in **Looking Direction** mode; this prototype had it backwards (delay-gated trigger only in Velocity-Direction-idle, continuous camera-chasing even while idle in Looking-Direction) - both now match real ALS's structure, with the Velocity-Direction-idle usage kept but explicitly labeled as a non-ported pragmatic addition.
  - **Movement acceleration/deceleration/friction are NOT portable**: real ALS's `GetMaxAcceleration()`/braking/friction all read a per-character `UCurveVector` (`CurrentMovementSettings.MovementCurve`) keyed by normalized speed - fully data-driven, no fixed numbers exist anywhere in the C++ source to port. `ACCELERATION`/`DECELERATION` remain flat constants, now documented as a known simplification rather than an oversight.
- **`ALSMathLibrary.cpp` ported** (the whole file read; 2 of its 6 functions had real work to do here, the other 4 documented in the tracker table above with reasons). The mantle's headroom guard was an invented zero-width downward raycast; it's now real ALS's `CapsuleHasRoomCheck` — a sphere sweep of the capsule's own radius along its cylindrical segment, tracing the capsule's exact volume, at the capsule centre derived via the real `GetCapsuleLocationFromBase` with `ALSMantleComponent.cpp`'s own 2cm lift.
  - **Real Godot-vs-Unreal API gotcha found doing this, worth remembering:** `PhysicsDirectSpaceState3D.cast_motion()` is NOT equivalent to Unreal's `SweepSingleByChannel`. It reports only collisions *newly entered during* the motion and silently returns `[1.0, 1.0]` for a shape that already overlaps something at its start position — verified directly, not assumed: a start position with **4 confirmed `intersect_shape` overlaps** still reported a completely clear sweep. Unreal surfaces exactly that case as `bStartPenetrating`, which ALS explicitly checks alongside `bBlockingHit`, so the one C++ line needs **two** Godot queries (`intersect_shape` at the start, then `cast_motion` for the sweep). A first version of the port used `cast_motion` alone and was wrong.
  - **Also a testing-discipline repeat:** the first version of the verification test "passed" its ceiling-rejection case for the wrong reason — the ceiling slab happened to sit in the path of the mantle's *downward probe ray*, so the mantle was rejected by the ledge-height check and never reached the room check at all. Same class of false pass as the stair-climbing "zero flicker" test earlier in this task. Fixed by placing every obstacle at `z >= 3.6`, clear of the probe ray at `z = 3.3`, so a rejection can only have come from the room check. Final test: both existing ledges still mantle, a low overhang is rejected (mid-sweep detection), a 0.5m slot narrower than the 0.7m body is rejected (start-penetration detection — the case a zero-width ray sails straight through), and **both rejection cases mantle again once the obstacle is removed**, ruling out a permanently-stuck false pass.
- **`ALSCharacterAnimInstance.cpp` deep pass done** (full accounting in the tracker table above - what was ported, what's curve-locked, what's portable-but-deliberately-skipped, and the two known-portable items still TODO). Two concrete things came out of it and are now in:
  - **`AimYawRate` is now a real cached per-frame value**, computed once at the top of `_physics_process` as `abs(Δaim_yaw)/delta` exactly like `ALSBaseCharacter.cpp`'s own `CacheValues` does, instead of each consumer deriving its own. Two ported systems read it (the turn-in-place delay gate and the rotation rate below), which is precisely why the real one caches it once.
  - **The grounded rotation rate is no longer a flat constant.** `ALSBaseCharacter.cpp::CalculateGroundedRotationRate` is `RotationRateCurve(mapped speed) * GetMappedRangeValueClamped({0,300},{1,3}, AimYawRate)`. The curve half still isn't portable (`ROTATION_SPEED` stands in), but the **aim-yaw-rate multiplier half is a genuine portable number** and is now applied: the body rotates up to 3x faster while the camera is being whipped around, so fast camera movements feel responsive without making slow ones twitchy. Previously the whole thing was one flat `ROTATION_SPEED`.
  - Verified headlessly, 5/5: the multiplier measurably accelerates convergence (88.43° vs 90.00° toward a 90° target over 30 fixed steps) without overshooting; idle turn-in-place still fires on a held 90° camera **after ~0.27s, matching ALS's own mapped delay for 90° (0.75 × 45/135 = 0.25s)** rather than merely "firing at some point"; and a 20° camera offset (under the real 45° `TurnCheckMinAngle`) correctly never fires.
- **Spine twist ported** (`AimingValues.SpineRotation`) — new `tests/manual/als_locomotion_prototype/prototype_spine_twist_modifier.gd`, a `SkeletonModifier3D` that keeps the character *looking* toward the camera while its feet stay planted. This is what makes the idle turn-in-place ported above read as deliberate instead of as a body frozen facing the wrong way, so the two belong together.
  - **Reading the AnimBP rather than trusting the C++ comment next to it changed the implementation.** `UpdateAimingValues` computes `SpineRotation.Yaw = AimingAngle.X / 4.0` and its comment says the divisor is the number of "spine+pelvis bones" — but `ALS_AnimBP` actually applies that value via `Transform (Modify) Bone` on **spine_01, spine_02, spine_03 only, not the pelvis**. Following the comment would have twisted the hips and with them the legs, fighting the foot IK. Three bones each taking a local quarter compounds to **0.25 / 0.50 / 0.75** of the aim angle in skeleton space — note the total is deliberately 0.75, not 1.0: real ALS does not fully counter-rotate the torso to face the camera.
  - **Deliberately a prototype-local class rather than reusing `PlayerLookPoseModifier`**, even though that existing modifier already does exactly this mechanic (and its snapshot-the-animation-pose-first-then-premultiply-in-skeleton-space technique was copied, being already proven). Its weights — `0.60/0.80/1.00` on the same three bones — are this project's own tuning for the shipped player, not ALS's; retuning them to ALS's numbers would have changed real gameplay to serve a sandbox.
  - Attached **after** the foot IK modifier on purpose: `SkeletonModifier3D`s run in sibling order and this one writes absolute global poses, so running it second means it snapshots a pose foot IK has already adjusted rather than one foot IK then moves out from under it. It only rewrites `basis`, never `origin`, so it can't fight foot IK's pelvis-height work either.
  - Gated to Looking Direction, matching `UpdateAimingValues`' own `!RotationMode.VelocityDirection()` check (in Velocity Direction real ALS leans on an aim offset toward movement input instead, which this prototype has no equivalent of).
  - Verified headlessly, 11/11. The load-bearing check was the **sign** — measured in world space via the change in yaw of a fixed vector carried by each bone, which is independent of both the bone's own axis convention and the visual node's 180° offset, rather than reasoned about (facing-axis reasoning has been wrong twice in this task). A +60° aim delta produces +15/+30/+45° on Spine/Spine1/Spine2, exactly ALS's cumulative weights; a −60° delta mirrors to −45°; Velocity Direction zeroes the twist and Looking Direction re-enables it.
  - **Gotcha, cost a hung test run**: adding a new `class_name` needs the Godot global class cache rebuilt (`--headless --editor --quit`) before any `--script` run can resolve it. Without that the prototype fails to parse, the test's `find_child` cascades on null, and the run hangs instead of erroring out.
- **Asset-side pass: the real numbers that were never in the C++ at all.** Prompted by asking whether the Blueprints had been converted (they hadn't). Two reads off the running editor produced more portable fidelity than several C++ files had:
  - **Real gait speeds** from `MovementModelTable` — 175/375/650 cm/s. Earlier in this task the C++ read concluded "no universal speed numbers exist in source", which was true but stopped one step short: they exist in the DataTable the C++ reads. This also exposed that the prototype had **two gaits where ALS has three**, and that ALS's default gait is *Running* — so the no-modifier speed went from an invented 3.0 to a real 3.75, with walk (1.75) added on a runtime-registered modifier key (the key is this prototype's choice; only the speeds are ALS's, and it's registered at runtime rather than written into `project.godot` so the sandbox can't change input for the real game).
  - **Real mantle trace settings** from the `MantleComponent` Blueprint — and this **disproves the standing assumption**, carried from ALSHost's own `AGENTS.md`, that the plugin's non-AnimBP Blueprints are thin config subclasses. The C++ declares the struct; the Blueprint holds every number. Ported the real Grounded (250/50/75) and Falling (150/50/70) profiles, which the prototype had collapsed into one invented set — so it now has ALS's real behaviour that a standing mantle reaches 2.5m but a mid-jump catch only reaches 1.5m.
  - **Re-reading `ALSMantleComponent.cpp`'s actual trace construction then corrected the port twice over.** The forward probe is not a low sphere but a **tall capsule spanning the entire mantleable height band** (half-height `1cm + (Max-Min)/2`, centred at `(Max+Min)/2`), which is why real ALS needs no "probe below the lowest ledge" trick to catch ledges of any height — the trick this prototype had invented to work around exactly that. Its start is also offset **backward by one trace radius**, so `ReachDistance` means real reach rather than reach-plus-a-radius. The downward probe starts from the wall impact point pushed **15cm into the surface along its own normal**, landing on the ledge top rather than skimming the edge. ALS also rejects a hit it could simply walk up (`IsWalkable`) — a mantle needs a wall, not a ramp — now ported against `floor_max_angle`.
  - **A test failure that was the test being right and the code being wrong.** An "out of reach at 0.85m" case failed while everything else passed. The tempting read was a bad expectation, since a 30cm-radius probe does reach 30cm past its centre — but that reasoning is exactly what the backward offset exists to cancel, and finding it meant the expectation was correct and the implementation was missing a step. Re-verified 11/11 after porting it, including the grounded-vs-falling split rejecting the 1.8m ledge mid-jump while still allowing it standing.
  - **`ALS_CharacterBP` read too, and it moved two more real defaults across**: ALS's `DesiredRotationMode` is **LookingDirection** (this prototype defaulted to Velocity Direction - just wrong), and `ThirdPersonFOV` is **90** against Godot's `Camera3D` default of 75, so the framing had been visibly tighter than ALS's. It also independently confirmed three things already believed: `DesiredGait = Running`, that `MovementModel` points at the "Normal" table row used above, and the 700/1000 breakfall/ragdoll velocities matching the earlier C++ read.
  - **The broader lesson: "read the C++" was not the same as "converted".** Three separate things this task had recorded as unportable or invented - gait speeds, mantle trace settings, and the default rotation mode/gait - were all sitting in plain sight on the asset side, because ALS's C++ deliberately declares structure and defers values to DataTables and Blueprint class defaults. The `AGENTS.md` heuristic that the plugin's non-AnimBP Blueprints are "thin config subclasses" is true about their *logic* and misleading about their *data*.
  - No regressions: the rotation/turn-in-place (5/5), spine-twist (11/11) and mantle (11/11) suites all still pass, plus a clean 90-frame scene smoke run.
- **Jump play rate ported** (`OnJumped`'s `GetMappedRangeValueClamped({0,600},{1.2,1.5}, Speed)`) - a jump taken at speed plays its animation faster so a running jump doesn't read as a standing one. Godot has no per-state play rate, so the Airborne state became an `AnimationNodeBlendTree` wrapping the existing blendspace with an `AnimationNodeTimeScale`, driven via `parameters/Airborne/JumpTimeScale/scale`. Set once at the moment of jumping exactly as ALS does, not tracked continuously. Verified exact: 1.2 standing / 1.35 at half speed / 1.5 at the 600cm/s cap.
- **Crouch implemented** - the first of the "feature subsystems with no equivalent", built entirely from real ALS values:
  - **Clips**: `ALS_CLF_Pose` + `ALS_CLF_Walk_F/B/L/R` retargeted onto Y Bot. ALS's crouch set is **4-point (pure F/B/L/R strafes, no diagonals)**, unlike the 6-point radial standing set - so the crouch blendspace is genuinely a different shape, not a scaled copy.
  - **Speeds** from `MovementModelTable`'s Crouching column: **150/200/300 cm/s**. ALS keeps all three gaits while crouched rather than collapsing to one speed.
  - **Capsule**: ALS's real `CrouchedHalfHeight` is **60cm** - it overrides UE's 40 default. Read via `obj dump` on the movement component, since `read_component_properties` can't see inherited native components (worth remembering). Gives a 1.2m crouched capsule; the camera drops by exactly the half-height delta, which is what ALS itself does (`BaseEyeHeight` 64→32 matches capsule half 92→60, both −32cm), so the camera needed no invented number of its own.
  - **Standing up is gated on `CapsuleHasRoomCheck`** at the standing height - the exact purpose that check serves in real ALS, and it was already ported for mantling, so crouch got it for free. Jumping from a crouch stands up first and is refused outright with no headroom.
  - **Structural choice**: stance is a `Blend2` INSIDE the Grounded state, not a sibling state. Real ALS does use separate `(N) Standing`/`(CLF) Crouching` states, but it also curve-blends the two base poses; a Blend2 is visually equivalent here and leaves every existing `playback.travel(&"Grounded")` call - turn-in-place, landing, mantle exits - working untouched. A sibling state would have silently broken all of them.
  - Foot IK suppression matches `PlayerBody`'s own condition (`crouched and on_floor and abs(input.x) > 0.5`) rather than suppressing for the whole crouch - only a crouched sideways strafe confuses the solver.
  - Verified 12/12, including the load-bearing case: a ceiling at 1.3m clears the crouched capsule but blocks standing, and standing succeeds again once removed.
  - **Two pipeline gotchas worth keeping**: (1) `retarget_cli.gd` cannot run under `--script` at all - it pulls in `PlayerBody`, which references the `PlayerProfile` autoload, and **autoloads are not registered in `--script` mode** (the same limitation that breaks `--check-only`). Retargeting has to be driven from a scene run instead. (2) A `CSGBox3D` added mid-run does not reliably register collision in time - it silently made a ceiling-blocks-standing case "pass" by not existing. Use `StaticBody3D` + a real shape for collision fixtures in tests; this had already been misread once earlier in this task.
- **Camera system ported** - the last conversion item. `ALSPlayerCameraManager.cpp`'s `CustomCameraBehavior` is fully portable as an algorithm, and is now implemented step for step in `_update_camera()`.
  - **Structural change**: the camera is no longer parented to the body. ALS's camera manager positions it in world space every frame with its own lag, which a parented node cannot express; `_camera_pivot` survives only as the control-rotation holder. Control **pitch** is also new - the old orbit cam was yaw-only.
  - **Where the values came from, and why reading the C++ alone would have failed.** Every tuning number in that function is `GetCameraBehaviorParam(name)` - an animation CURVE on the Camera_Skeleton rig. The C++ gives the structure and not one usable constant. The numbers were read off `ALS_PlayerCameraBehavior`'s own `ModifyCurve` nodes: pin defaults carry the values, and each node's `CurveNames` array carries which curve each slot maps to. **`CurveNames` is invisible to `read_function_graphs`** - it needed `run_python`'s `inspect_graph_node`, which is the tool that made this whole port possible. Without it the values were an unlabelled list of numbers.
  - Real values now in: **Velocity Direction** `CameraOffset(-325, 0, 20)`, `PivotLagSpeed(5, 5, 15)`, `PivotOffset_Z 30`; **Looking Direction** `CameraOffset(-280, 70, 35)`, `PivotLagSpeed(8, 8, 15)`, `PivotOffset_Z 25` - note the `+70` is ALS's real over-the-shoulder offset; `RotationLagSpeed 20` (identical across all three ALS camera states); trace radius **15cm** from `GetThirdPersonTraceParams`. ALS's third state, Aiming (`-200, 70, 35`, lag `15,15,15`), is recorded but unused - no aiming mode here.
  - **The pivot target is the midpoint of the Head and root sockets**, not a fixed height (`ALSCharacter::GetThirdPersonPivotTarget`). That single detail is why ALS needs no crouch-specific camera rule - the head drops, so the pivot drops - and the crouch camera-lowering rule added earlier was removed as redundant because of it.
  - `CalculateAxisIndependentLag` is the clever bit and is ported exactly: lag is applied in the camera's **yaw-only** frame (ALS explicitly zeroes roll and pitch), so looking up or down never changes which way the camera lags - which is what lets ALS use a much slower vertical lag (15) than horizontal (5) without it feeling wrong. `FInterpTo`'s exact form (a clamped fraction of remaining distance, not exponential smoothing) is matched too, since the curve-tuned feel depends on it.
  - Verified 11/11: camera unparented, FOV 90, pivot mid-body, 3.25m boom in Velocity Direction with no lateral offset, 2.8m + 0.7m over-the-shoulder in Looking Direction, rotation lag that provably lags one frame and then converges, and collision pullback that pulls in to 1.16m against a wall and returns to 3.27m once it's gone.
  - One test failure was the TEST being wrong, worth noting: its Velocity-Direction assertions reported exact Looking-Direction values, because the prototype now correctly defaults to Looking Direction (ALS's real `DesiredRotationMode`, ported earlier). The test had assumed the old default.
- **RESOLVED - crouch pose "flattened by foot IK" was a wrong diagnosis; real bug was a wrong retarget argument, and it's fixed.** Originally recorded here as an open foot-IK defect, based on a test that appeared to show the crouched pelvis being dragged back up toward standing height. That diagnosis was wrong on two counts, corrected in order:
  1. **The test itself was broken**, not the game: it sampled the skeleton after only 12 physics frames, not enough for the stance blend to reach 1.0 and for `AnimationTree` to actually publish the pose - so it was reading a stale (still-standing) skeleton and reporting the same stale numbers regardless of what was changed. Three different real fixes (`set_pose_suppressed`, then `active = false` on the foot IK modifier) all produced byte-identical output, which was the tell. Fixed by settling 40 frames instead of 12; with that, crouching correctly measures head dropping from 1.446m to 0.951m above the feet. **Confirmed by direct test that `active = false` was never actually needed** - with the real bug (below) fixed and the foot IK modifier left fully active throughout crouch, the same 1.446 -> 0.951m measurement holds. Reverted; foot IK now stays on through crouch, with only strafe-suppression (`PlayerBody`'s own exact condition, `crouched and on_floor and abs(input.x) > 0.5`) remaining.
  2. **The real bug, found via live user reports** ("crouch is working except for the arms, crossed behind the back - like sprint did before" - the *same* symptom across two unrelated features is what pointed at a shared cause rather than two separate bugs): all 6 clips added this session (5 crouch clips + `ALS_N_Sprint_F`) were retargeted with the WRONG source role map. `retarget_cli.gd` has its own hand-built `ALS_SOURCE_ROLE_MAP`; a one-off retarget scene used `PlayerBody.BONE_MAP` instead, on the mistaken assumption the two were interchangeable (they're both "ALS bone name -> canonical role", but built independently and not kept in sync). They differ in exactly one key: `ALS_SOURCE_ROLE_MAP` has `"head" -> "Head"` (lowercase, the real ALS source bone name); `BONE_MAP` has `"Head" -> "Head"` (capitalized, matching nothing on that skeleton). With `head_source` unresolvable, `HumanoidRetargeter._skeleton_height(src, hips, head)` fell through to its `0.0001` floor, `arm_position_scale` (in `_match_arm_skeleton_positions`, which scales the desired-wrist target fed to the two-bone arm IK) exploded, and the solver aimed at an effectively-infinite point and clamped - throwing the arms out behind the back and mirroring them. Legs/spine were unaffected since only the arm-matching pass uses that scale, which is exactly why crouch's capsule/speed/room-check/blend and sprint's speed/play-rate all tested correctly the whole time - only the arms were wrong, and no automated test in this task was checking arm pose specifically.
  - **Fix**: re-retargeted all 6 clips with `ALS_SOURCE_ROLE_MAP` (`assets/models/als_retarget_test/{ALS_CLF_Pose,ALS_CLF_Walk_F,ALS_CLF_Walk_B,ALS_CLF_Walk_L,ALS_CLF_Walk_R,ALS_N_Sprint_F}_on_ybot.res`). Verified by measuring hand X positions before/after: clean left/right separation with no overlap post-fix, matching the source clip's own convention.
  - **Lesson, worth repeating for any future retarget in this project**: two source role maps that look identical at a glance (same shape, mostly-overlapping entries) can differ in exactly one key, and a wrong map does not fail loudly - it degrades gracefully into a plausible-but-wrong pose (clean-looking legs/spine, broken arms only), the kind of bug that survives casual review and needs an actual measurement (or, as here, a second independent case with the identical symptom) to catch. Always retarget ALS source clips via `retarget_cli.gd` itself (or its own `ALS_SOURCE_ROLE_MAP` constant, imported not copied) rather than reaching for a different project bone map that merely looks equivalent.
  - Also worth repeating: an in-game runtime probe can be unreliable in ways that look like real findings - one measurement here reported `blend=0.00` while supposedly sampling mid-sprint (never actually measuring sprint at all), and the same clip's hand X measured with opposite sign in-game vs. offline. Two dead-end hypotheses (a per-clip facing mirror, an out-of-phase blend needing `AnimationNodeBlend2.sync`) were chased before the real cause surfaced - `sync = true` on the sprint/stance blends was kept since it's correct regardless (a 0.8s and a 0.6s cycle blending without sync WILL drift out of phase over time), but it was not what fixed this bug.
- **Fixed: Airborne jump/fall blend was a hard step, not a blend.** User report ("the blend with the jump and falling is not well done") after visually reviewing the scene. Root cause was in the original Phase 5 scaffold, not this session's changes: `AirBlend`'s `blend_position` was driven by `signf(_body.velocity.y)`, which only ever outputs -1, 0, or +1 - so the blendspace snapped instantly between the jump and fall clips the moment vertical velocity crossed zero (the arc's apex) rather than easing through it, despite genuinely being a 2-point `AnimationNodeBlendSpace1D` that COULD blend. Fixed by driving it off actual velocity, clamped to a real range (`AIR_BLEND_VELOCITY_RANGE = 2.0` m/s, this prototype's own tuning value, chosen to ease through the top of a normal jump's arc without visibly stretching into launch or landing) instead of its bare sign. Verified: blend position now takes 6+ distinct values across a sampled velocity sweep (the old code could only ever produce 3), a mid-range velocity maps to itself rather than to a step, and full jump/fall speed still correctly clamps to the blendspace's endpoints. No regressions across all 6 existing suites.
- **Real jump physics, air lean, and land prediction ported.** Prompted directly by "the jump is not the same as the one implemented on ALSHost" - investigated what was actually different rather than just re-tuning by feel.
  - **Jump physics were invented and genuinely far off, unlike almost everything else ported this session.** Read live via `obj dump` on `CharMoveComp` (same inherited-native-component gotcha as `CrouchedHalfHeight`): real ALS `JumpZVelocity=420` cm/s (4.2 m/s) and `GravityScale=1.0` against UE's standard 980 cm/s² world gravity (9.8 m/s²) - not overridden anywhere. This prototype had 6.0/18.0. Worked out the practical difference: real ALS's jump takes ~0.43s to apex vs. this prototype's previous ~0.33s for a similar peak height - a real ALS jump hangs in the air noticeably longer, which is very plausibly the exact thing that read as "not the same". Ported directly (`JUMP_VELOCITY=4.2`, `GRAVITY=9.8`); `AIR_BLEND_VELOCITY_RANGE` (this prototype's own airborne-blend-ease tuning, not a real value) scaled down proportionally from 2.0 to 1.4 to match.
  - **Air lean ported** (`CalculateAirLeanAmount` + its `FInterpTo` in `UpdateInAirValues`): local (forward,right) velocity ÷ 350cm scaled by a fall-speed-keyed curve, interpolated at the real `Config.GroundedLeanInterpSpeed=4.0` (ALS reuses its grounded lean speed for air lean too - there's no separate airborne one). The real `LeanInAirAmount` curve is genuinely non-monotonic (dips to -0.93 around -22.5 m/s fall speed before easing back toward 0 near terminal velocity) - read live via `run_python`'s `inspect_asset` (`FloatCurve` keys) and baked exactly as a Godot `Curve`, not approximated or smoothed.
  - **Land prediction ported** (`CalculateLandPrediction`): real fall-speed cutoff -2.0 m/s, trace length mapped from fall speed over a real range, and a genuine capsule sweep toward the ground - Godot's `cast_motion` safe-fraction is exactly UE's `HitResult.Time` for this purpose. The real `LandPredictionBlend` curve (1.0 at hit-fraction 0, easing to 0.0 at fraction 1) was likewise read live and baked exactly.
  - **What's a real port vs. this prototype's own choice, stated plainly**: the formulas, interp speed, and both curves are byte-for-byte real ALS data. What's NOT ported is real ALS's actual CONSUMER of these values - it blends toward authored lean-pose and landing-recovery clips inside the AnimGraph, which needs AnimGraph-level node access this port doesn't have (the same situation as turn-in-place's 90°/180° clips, which at least exist as retargeted assets - lean-pose clips were never even sourced). Applied instead as a direct procedural tilt of the whole character node (`LEAN_MAX_ANGLE_DEG=15°`, this prototype's own choice), with land prediction fading that tilt back toward upright as the ground approaches - a real, if partial, piece of "anticipating landing," not a claim of matching the real pose blend.
  - **Real Godot gotcha, cost two rounds of test failures**: `Curve` silently clamps BOTH its X domain (`min_domain`/`max_domain`, default `[0,1]`) and its Y value range (`min_value`/`max_value`, ALSO default `[0,1]`) at `add_point()` time, with no error - every interior point of the lean curve came back with its real Y value silently zeroed, because `-0.93` fell outside the default `[0,1]` Y range, and this looked at first exactly like a tangent-argument bug rather than a range-clamp one. Isolated by testing `add_point` with and without tangent args in a standalone script, which showed the Y-zeroing was identical either way - ruling out tangents and pointing at the domain/range instead. Both need to be explicitly widened to the curve's real range BEFORE adding any points.
  - **Also caught**: an early "land prediction is non-zero when falling fast near the ground" test failure was the test's own fault, not the code - it teleported the body and immediately read `_land_prediction` in the same frame, before `is_on_floor()` had a chance to update via a real `move_and_slide()` call, so the function's early-return-if-grounded check fired on stale state. Confirmed by a standalone diagnostic that awaited real physics frames after the teleport and got a correct nonzero result immediately. Fixed by awaiting 3 physics frames before reading.
  - Verified 11/11 (curve values exact against the live-read assets, lean produces a real nonzero tilt while airborne and decays back to upright once grounded, land prediction correctly gates on the fall-speed cutoff and responds to sweep distance). No regressions across all 6 other suites.
  - **Not attempted, and said so rather than faked**: full breakfall-roll / ragdoll-on-hard-landing. Real thresholds are known (`BreakfallOnLandVelocity=700`, `RagdollOnLandVelocity=1000` cm/s, from `ALS_CharacterBP`'s class defaults, read earlier this session) and `ALS_CharacterBP` even names the real get-up montages (`GetUpFront_Default`, `GetUpBack_Default`, etc.) - but this is a genuinely large feature (physical bone ragdoll simulation, capsule enable/disable, get-up animation retargeting and blending), disproportionate to a parameter port, and was flagged for a scoping decision rather than attempted as a token/partial version.
- **Mantle's real `PositionCorrectionCurve`-driven execution ported - the last big documented simplification in mantling is now gone.** Prompted directly by "is [mantle] well implemented? some parts clip and maybe too rigid" - rather than re-tuning the invented `smoothstep()` lerp by feel, read the actual mechanism `ALSMantleComponent.cpp::MantleUpdate` uses and ported it.
  - **What the real system actually does, once read past the earlier "it's just a curve-driven blend" summary**: `MantleUpdate` samples a `CurveVector` (X=`PositionAlpha`, Y=`XYCorrectionAlpha`, Z=`ZCorrectionAlpha`) by real ELAPSED SECONDS (not a normalized 0-1 fraction), and uses it to blend the character's position through an intermediate reference point - `MantleAnimatedStartOffset`, a point behind (`StartingOffset.Y`) and below (`StartingOffset.Z`) the ledge, matching where the mantle clip's own root motion is actually authored to begin - before finally blending that whole offset-based position toward the true landing spot as `PositionAlpha` rises. Horizontal (XY) and vertical (Z) correction run on their OWN independent timing per the curve's Y/Z axes (XY arrives faster than Z for the 1m asset), which is the actual mechanism shaping the climb - not a single uniform lerp treating every axis identically, which is what made the old version read as rigid.
  - **Real values, read live**: `StartingOffset` per asset from `MantleComponent`'s class defaults (`Y=65cm/Z=100cm` for the 1m asset, `Y=65cm/Z=200cm` for the 2m asset - already partially known from the earlier mantle-trace-settings read, now actually used). The `PositionCorrectionCurve` itself (`Mantle_1m`/`Mantle_2m`, both `CurveVector` assets) needed `obj dump` + log grep rather than `run_python`'s `inspect_asset` - that tool's generic property dump only surfaced axis 0 of the `FloatCurves[3]` fixed array, silently dropping Y and Z. **Real, non-obvious finding**: `Mantle_2m`'s curve domain genuinely extends to 2.1 (seconds), not `[0,1]` like `Mantle_1m` - the two assets don't share a normalized-time convention, confirming the curve really is sampled by real elapsed seconds (matching `MantleTimeline->GetPlaybackPosition()`), not a fraction.
  - **Faithful in math, not in transform machinery**: real ALS composes this via `FTransform`/`TransformAdd`/`TransformSub` algebra in the ledge's own local rotation frame; this prototype does the equivalent directly in world Cartesian space, which is exactly equivalent since the target's only real rotation component is yaw (mantle facing is already handled as a separate yaw lerp here, same as before).
  - Verified 5/5 in the permanent suite (added to `tests/automated/als_locomotion_prototype/locomotion_test.gd`'s mantle section): both curves' real key values sample correctly (including the 2.1s domain), and - the two checks that actually matter - a mantle still lands EXACTLY on the real target regardless of the new path math (the curve reshapes the journey, not the destination; a regression here would have silently broken every existing mantle test that only checks the end state), and the mid-climb position now measurably diverges from a naive straight lerp (0.49m at 30% elapsed) - confirming the real per-axis timing is doing something, not just decorative. No regressions across the full 62-check suite.
  - **`StartingPosition`/`PlayRate` also ported** (closing the one gap noted above as left out - re-opened after the user pushed back with "we need to port it all, it must work the same"). Real per-asset `LowHeight`/`HighHeight`/`LowStartPosition`/`HighStartPosition`/`LowPlayRate`/`HighPlayRate` (from the same `MantleComponent` class-defaults read; `HighStartPosition`'s default of `0.0f` confirmed against the struct default in `ALSCharacterStructLibrary.h`, since the CDO dump omits properties left at their default). `StartingPosition` lets a real ledge SHORTER than an asset's own designed max skip ahead into the correction curve instead of always starting at 0 - previously every mantle here always sampled from t=0 regardless of the actual gap, silently discarding real data already on hand. `PlayRate` turned out to be a per-asset CONSTANT (both range endpoints equal - 1.0 for the 1m asset, 1.2 for the 2m one), applied to both the position-correction curve sampling AND the clip's own playback speed (via the same `AnimationNodeTimeScale`-wrapping technique already used for Airborne/Grounded). Verified 4/4 additional checks: both real PlayRate constants exactly, `StartingPosition=0` at an asset's own `HighHeight` (full curve), and a real partial skip-ahead (`0.16`, matching hand-calculation exactly) for a mid-range height.
  - **First investigated as possibly-inherent, then found to be a real, fixable bug after the user pushed back a second time** ("check what else is missing" after "must work the same"). Confirmed real ALS's `MantleUpdate` doesn't sweep for collision either (`SetActorLocationAndTargetRotation` → `AActor::SetActorLocationAndRotation`, `bSweep` defaults **false** - a plain teleport, same as this prototype's own position write), which is true and stayed true - but reading further into `ALSMantleComponent.cpp::Start()` past where the earlier pass had stopped (specifically step 3, computing the actual `TargetTransform`) surfaced a real, separate bug: **this prototype was adding a second, entirely invented 0.5m forward offset (`MANTLE_LEDGE_FORWARD_OFFSET`) on top of the real one.**
  - Real ALS's landing target position IS `CapsuleLocationFBase` - the downward-trace hit point (`DownTraceLocation`, already pushed 15cm into the ledge along the wall normal - `DownwardTraceEnd += InitialTraceNormal * -15.0f`) lifted to capsule-centre height. **No further forward travel is added anywhere in the real function.** This prototype's `ledge_top` already correctly included that real 15cm push (`MANTLE_DOWN_TRACE_NORMAL_OFFSET`, ported earlier), but then computed `landing_spot := ledge_top + facing_dir * MANTLE_LEDGE_FORWARD_OFFSET` - an extra, invented 0.5m with no real counterpart, dragging the character roughly 4x deeper across the ledge surface (through far more of its volume) than real ALS ever does on every single mantle. Removed entirely; `landing_spot` is now just `ledge_top`, matching real ALS exactly.
  - A genuinely useful side effect of the fix, worth noting for its own sake: real ALS's ledge-DETECTION trace and its room-CHECK sweep turn out to happen at the exact same real XZ column (`CapsuleLocationFBase` IS `DownTraceLocation`) - something the earlier, offset-inflated version of this port obscured, since its artificial 0.5m gap kept the two spatially separate. Two room-check test fixtures (`tests/automated/.../locomotion_test.gd`) had to move to the real landing spot's coordinates as a result, and one scenario ("low overhang, mid-sweep rejection") turned out to no longer be independently constructible now that the geometry is honest - a low overhang at the real landing column is now, correctly, also in the path of ledge detection itself, exactly like real ALS. That code path (`_capsule_has_room`'s `cast_motion` branch) is still covered for real, just via the crouch section's "low ceiling blocks standing up" case, which exercises the identical function.
  - Verified via the same landing-exactness and mid-climb-divergence checks as the curve port above, now against the corrected geometry, plus the room-check fixtures re-verified at their new real coordinates (measured directly via a diagnostic, not re-guessed by hand a second time after an initial guess missed). 66/66 across the full suite.
- **Mantle hand IK added (prototype-only feature, explicitly NOT an ALS port).** User pushed on "the character doesn't stay attached to the climb point" a second time after the real bug fixes above, asking "is there no other Godot similar way?" - the honest answer was yes: `SkeletonIK3D`-style bone-chain pinning, which this exact project already does for feet (`PlayerFootIKModifier`). Built the hand equivalent following that established pattern.
  - **Root cause, measured before building anything**: sampled both hands' distance to the wall plane across a full low-ledge climb. The LEFT hand tracks the ledge acceptably (within 2cm) for roughly half the climb (t=0.33 to t=0.83) - the curve+animation combination genuinely works, partially - but the RIGHT hand never settles as cleanly, and both hands end 18cm past the wall at completion. Conclusion: the real `PositionCorrectionCurve` was authored by an Epic animator against the exact proportions of the original UE Mannequin; retargeting preserves the clip's relative motion faithfully but can't make a differently-proportioned Y Bot's hand arrive at exactly the point the curve assumes - a retarget-fidelity ceiling, not a further missing ALS value (confirmed separately: real ALS has no runtime hand IK during mantling at all, it relies entirely on this same animator-tuned assumption).
  - **New file** `tests/manual/als_locomotion_prototype/prototype_mantle_hand_ik_modifier.gd` - a `SkeletonModifier3D` pinning both hands toward the mantle's own real grip point (`_mantle_target_pos`, the same point the position-correction curve targets). Deliberately far simpler than `PlayerFootIKModifier` (a ~1000-line system built for continuous ground contact with gait/stair/landing state) - a mantle is one bounded, one-shot event, so this only needs the two-bone analytic IK solve itself. Reused rather than reinvented: `HumanoidRetargeter._solve_two_bone_ik`/`_swing_between`, the same functions already proven at retarget-bake time for arm-matching, called here at runtime instead via `get/set_bone_pose_rotation` in place of writing Animation keys.
  - **A real design bug found via direct measurement, not assumed correct**: the first version pinned toward the ledge point at a weight driven purely by the correction curve's own timing, regardless of whether the target was physically reachable. Since the ledge point is often still 1m+ from the shoulder while the body is mid-climb (climbing to a high ledge necessarily starts out of arm's reach - that's the whole point of the climb), this stretched the arm toward an unreachable point instead of gripping - measured directly: the right hand's distance to target got WORSE (up to 1.09m) than running with no IK at all. Fixed by gating the effective per-arm weight on actual reachability (`reach_factor`, fading smoothly to 0 as distance exceeds arm span rather than a hard cutoff), so the pin only engages once physically plausible - matching how real climbing actually works (you don't grip a ledge before you're close enough to touch it).
  - **A related measurement pitfall worth remembering**: comparing raw hand-to-target distance before/after the reachability-gate fix looked identical at first, which briefly looked like the fix had no effect. It doesn't, because that metric is fundamentally bounded by shoulder-to-target distance minus arm length regardless of any gating logic - the fix changes whether the arm looks rigidly overextended while far away, not how close the hand numerically gets once it's already geometrically constrained. Caught by reading the gate's own internal values directly (`reach_factor`/`effective_weight` per frame) instead of trusting the outer metric alone.
  - Verified 3/3 in the permanent suite: IK weight stays low (<0.1) immediately after triggering while still far from the ledge, rises above 0.5 at some point once genuinely close, and fades back out (<0.3) by the climb's final 20% as the body commits fully and hands would naturally release. No regressions across the other 8 sections (69 checks total).
- **Permanent automated test suite added**: `tests/automated/als_locomotion_prototype/locomotion_test.gd` - run via `godot --headless --script res://tests/automated/als_locomotion_prototype/locomotion_test.gd` (exit 0 = pass, 1 = failure, full pass/fail printed to stdout). Prompted directly by asking whether every action-triggering-animation should have an automated test, given this session's own track record: several real regressions (the sprint retarget bone-map bug, the crouch pose masked by a stale test read, the Curve domain/value clamp) were only caught because a scratch test happened to be run again, not because anything was standing guard.
  - **This is a new pattern for the project** - every prior Godot test on this task (and everywhere else under `tests/`) was a `tests/manual/*.tscn` for eyeballing, or a throwaway `--headless --script` run written fresh and deleted after each check (9 of those were written just in this session). Consolidates all 9 into one file, 8 sections / 62 checks: rotation rate + turn-in-place, mantle (real trace settings + room check), spine twist (math verified even though currently disabled), camera, crouch + jump play rate, gait speeds + sprint blending, airborne blend easing, and jump physics + air lean + land prediction.
  - Each section re-instantiates the prototype scene fresh rather than sharing state, deliberately costing some runtime (~19s total for all 8) - several of the original scratch scripts chained scenarios in one scene instance, and a bug in an earlier scenario risked silently corrupting a later assertion's starting state.
  - Nothing in it was invented for this suite - every check is a real assertion that caught (or would have caught) an actual bug during this session's work; the "why" for each is in its own comment, cross-referenced to this status log rather than duplicated.
  - The scratch scripts it replaces (`test_capsule_room.gd`, `test_rotation_rate.gd`, `test_spine_twist.gd`, `test_mantle_profiles.gd`, `test_camera.gd`, `test_crouch_jump.gd`, `test_gait_blend.gd`, `test_air_blend.gd`, `test_air_lean_land.gd`) lived only in the session's scratchpad temp directory and have been deleted now that their coverage is consolidated here.
- **Idle secondary-motion sway: attempted properly, still not wired in.** Root-caused why `retarget_clip()` breaks on `ALS_N_SecondaryMotion` (it's an *additive* clip - a delta from rest pose, not an absolute pose - confirmed by nonsensical 78° arm rotations when treated as absolute) and wrote a real second retarget path, `HumanoidRetargeter.retarget_additive_clip()` (also exposed via `retarget_cli.gd additive=true`), that computes each bone's delta from ITS OWN rest pose and converts it into target-bone-local space. **This path is itself broken for a real fraction of bones** - verified two ways: `Hips` (zero authored motion in the source clip) produced a flat, constant 90° output at every single frame, a pure rest-convention-mismatch artifact with no connection to real motion; fixed by having the function skip any bone whose source track never actually deviates from rest (a `ROTATION_STATIC_THRESHOLD_DEG` pre-scan). But bones WITH real authored motion are equally exposed and this filter doesn't catch them: after the fix, `RightShoulder` still reached 183°, `RightUpLeg` 183°, `LeftUpLeg` 174°, several finger bones 60-94° - clearly still wrong for what's meant to be a subtle sway. The borrowed pre/post-basis conjugation (copied from the absolute-pose formula) isn't proven delta-preserving in general; a correct fix needs a genuine similarity-transform conjugation, not attempted given the risk of shipping something subtly wrong a third time. `retarget_additive_clip()` is left in the codebase (clearly flagged BROKEN in its own doc comment) as a real starting point for whoever picks this up next, but the baked resource was deleted and nothing references it - the idle pose still just holds `ALS_N_Pose` with no sway layered on top.
