[plugin]

name="MCP Editor Bridge"
description="Lets tools/character_editor_mcp control the already-open editor (open scenes, select nodes) over a local TCP socket, instead of only spawning fresh headless Godot processes."
author="survival-horror-fps"
version="1.0"
script="plugin.gd"
